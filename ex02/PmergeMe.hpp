#ifndef PMERGEME_HPP
#define PMERGEME_HPP

#include <vector>
#include <deque>
#include <iostream>
#include <sstream>
#include <string>
#include <ctime>
#include <stdexcept>
#include <algorithm>

// vector version
void mergeInsertVector(std::vector<int> &vec);

// deque version
void mergeInsertDeque(std::deque<int> &deq);

#endif